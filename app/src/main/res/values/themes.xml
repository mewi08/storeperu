<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StorePeru" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StorePeru" parent="Base.Theme.StorePeru" />

    <style name="Theme.StorePeru.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.StorePeru</item>
    </style>

    <style name="buttonDanger" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/danger</item>
        <item name="cornerRadius">0dp</item>
    </style>

    <style name="buttonPrimary" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="cornerRadius">0dp</item>
    </style>

    <style name="buttonInfo" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/info</item>
        <item name="cornerRadius">0dp</item>
    </style>

</resources>